# Architecture

CloudTune uses a clean, extendable monorepo structure:

- `backend/` for Express APIs, admin-ready modules, and service boundaries
- `mobile/` for the Flutter client
- `database/` for PostgreSQL schema and migrations
- `docs/` for design and implementation notes
